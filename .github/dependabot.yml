version: 2

updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "daily"

    groups:
      development-dependencies:
        dependency-type: "development"

    ignore:
      - dependency-name: "lodash"
        versions:
          - ">= 5.0.0"
